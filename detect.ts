import { ItemName, MaterialItem, SynthesisItem } from "./items.ts";

export const detectCircularSynthesisPathsBottomUp = (
  materiarItems: MaterialItem[],
  synthesisItems: SynthesisItem[],
  targetItemName: ItemName,
): SynthesisItem[][] => {
  return [];
};
